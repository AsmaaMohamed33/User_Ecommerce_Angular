import { NaionalIDPipe } from './naional-id.pipe';

describe('NaionalIDPipe', () => {
  it('create an instance', () => {
    const pipe = new NaionalIDPipe();
    expect(pipe).toBeTruthy();
  });
});
